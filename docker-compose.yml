services:
  bot:
    build: .
    container_name: "115-bot"
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data:/data          # 持久化 session + 日志
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
