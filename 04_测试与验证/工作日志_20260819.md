完成115-Telegram自动化方案调研：确认可行链路为TG私聊接收115链接 -> 任务队列去重/重试 -> 115转存 -> 重命名 -> 创建长期/永久分享 -> 发布到TG频道。优先建议基于开源P115-Share验证，项目已明确覆盖Telegram监听、115转存、长期链接生成、频道同步和Docker部署。115官方开放平台要求开发者审核并明确禁止未经书面同意使用机器人/自动脚本获取服务，故生产前应优先走OpenAPI；网页CK/非公开接口仅作受控测试并承担失效/风控风险。用户NAS环境可采用Docker部署，默认按测试频道、白名单、人工审核开关逐步上线。

已通过SSH在192.168.31.70完成部署：配置位于/vol2/1000/docker/115/docker-compose.yml，服务容器p115-share使用listeningltg/p115-share:latest，数据持久化到/vol2/1000/docker/115/data，端口8000，HTTP GET返回200，启动日志正常；首次默认管理员为admin/admin，需立即修改密码。已核对镜像1.1.3配置接口：支持TG Bot Token、频道ID、用户ID白名单、115 Cookie、115保存目录、直存目录等配置；敏感值应由用户直接在Web管理台填写，不通过聊天传输。用户询问是否可在聊天发送115 Cookie和TG Bot Token；已明确拒绝接收，要求通过NAS Web管理台直接填写，并建议若凭证曾经泄露则立即轮换。
TG机器人由 aiogram 实现，频道推送逻辑在 tg_bot.py L600-900：先把原消息文字/图片存为 metadata，转存成功后调用 broadcast_to_channels，使用 send_photo(caption=替换链接后的text) 推送；不支持自动查TMDB/下载海报/拼资源卡片，用户目标模板需要在外部独立发布适配层或 fork 项目实现。镜像内置 tmdb_service.py 用途仅为敏感词过滤(SensitiveMovie/TMDBConfig)，不参与频道消息生成。

方案B实现完成（独立卡片中间层 card-bot）：部署在 /vol2/1000/docker/115/card-bot/，基于 listeningltg/p115-share:latest 同镜像覆盖 entrypoint 运行 cardbot.py，复用镜像内 p115client（P115Service.save_and_share 完成转存+长期分享，返回带?password=的分享串，且该方法不依赖DB，可用cookie独立调用）。cardbot.py 已验证导入与卡片渲染，输出格式与用户目标模板逐行一致（标题/类型/TMDB/豆瓣/画质/集数/大小/链接/简介/标签）。115信息(文件名/大小)经 p115client.share_snapshot 尽力获取，失败降级；TMDB需 TMDB_API_KEY（可选），豆瓣尽力而为。HTTP 用镜像自带 aiohttp（镜像无 requests）。

2026-08-19 已填.env并拉起成功：容器 p115-card-bot 运行正常，日志确认「P115Client 初始化成功(账号ID: cardbot)」「115 网盘登录验证成功」「Bot Token 有效(@S7_nanshare_bot id=8705754894)」。关键修复两处：(1) compose 加 working_dir:/app 并在 cardbot.py 把 /app 与 /cardbot 插入 sys.path，否则 import app.services.p115 失败；(2) 在 import p115 前从 os.environ 删除 P115_COOKIE/P115_SAVE_DIR，避免 P115-Share 的 Pydantic settings 在模块级单例 P115Service() 导入时自动 init_client() 触发 no-running-event-loop 报错。已确认 P115-Share 容器侧 Bot Token 为 invalid（未轮询），故与 card-bot 无 getUpdates 冲突。

二轮迭代（用户首测发现3个问题）：(1) 没有测试模式开关 → 加 CARD_BOT_TEST_MODE（1=不调115、2=调115但不发频道）+ /card <name> 纯文本模板命令，无需115即可验模板；(2) fetch_share_info 用 client.share_snapshot（不存在的方法）导致 fallback 到 URL → 改用 client.share_snap_app 带鉴权版（proapi.115.com），失败再回退 share_snap（webapi）。P115ShareFileSystem 内部用 share_snap 走 webapi 会 405（IP 频控），必须直调 share_snap_app；(3) ENC_RE 加 DDP/DDP5.1/EAC3。用用户真实分享 https://115cdn.com/s/swsdnpn3wwq?password=m2f2 实测：解析出标题「Mystic.Nine.S01.2026.2160p.WEB-DL.HQ.DV.60FPS.H.265.DTS5.1-HiveWeb」→标题「Mystic Nine S01」、画质2160P、视频WEB-DL、编码H.265、大小168173567321B（=156.62GB，与用户给的截图数字一致）。卡片模板已与用户目标模板字段对齐；类型/TMDB评分/简介/海报需用户提供 TMDB_API_KEY（免费申请 https://www.themoviedb.org/settings/api）才能填充。

三轮修正（用户选测试方式 B=发真链接测模板）：发现命名坑——代码里 CARD_BOT_TEST_MODE=1 是「纯URL不查115」(会复现标题变URL)，=2 才是「查115真实信息但不转存不发频道」，用户要的「发真链接测模板」实际对应 mode 2。另外更严重：原 .env 与 compose 根本没配/没转发 CARD_BOT_TEST_MODE → 容器默认跑正常模式(真转存+发频道)，与「测试期不转存」相反。已修复：(1) .env 写入 CARD_BOT_TEST_MODE=2；(2) compose environment 块新增 `CARD_BOT_TEST_MODE: ${CARD_BOT_TEST_MODE}` 转发到容器；(3) docker compose up -d 重建。验证：容器内 printenv=2、115登录成功、TG_ALLOW_CHATS=8507969163(用户私聊放行)、直连 fetch_share_info 实测拿到真实 156.62GB 与正确解析。正式上线把该值改 0 即可。

TMDB 澄清与修复：用户实际上早已在 .env 填好 TMDB_API_KEY（32位干净值，聊天里贴的 `...ad1.env` 只是复制手误，文件里并无后缀）；容器内 printenv 长度=32，代理 APP_HTTP_PROXY=http://192.168.31.18:7890 生效。实测 tmdb_search("Mystic Nine") 正常返回 poster/rating/overview。发现真 bug：机器人用解析标题 "Mystic Nine S01"（带季标）搜 TMDB 返回 None，导致卡片仍缺海报/评分/简介。修复：parse_filename 末尾新增 `re.sub(r"\s*S\d{1,2}(?:-S?\d{1,2})?$", "", title_raw, flags=re.I)` 剥离尾部季标签；本地改后 scp 到 /vol2/1000/docker/115/card-bot/cardbot.py 并 docker restart p115-card-bot。复测 parsed_title="Mystic Nine" 且 TMDB 命中。

2026-08-19 用户首测：aiogram 3 的 send_photo 不接受裸 bytes 海报 → 触发「卡片发送失败」。修复：用 BufferedInputFile 包海报字节再发；本地改后 scp 同步 + docker restart。新容器无报错、115 登录成功。

五轮（核心需求：集数必须按 115 分享内真实文件数写）：新增 fetch_share_video_files(share_url) 递归用 client.share_snap_app(带 cid 翻页) 列出分享内所有真实视频文件名（规避 P115ShareFileSystem.from_url 的 405 频控；share_snap_app 返回 list 里 fc=0/字符串"0" 或 is_dir 为文件夹，fn 以 VIDEO_EXT 结尾为视频）。infer_episode_range(video_names, root_name)：提取 SxxEyy 按季分组取首末集 → 无完结标记写 `S01E01-E28`；含完结关键词(complete/完结/全集/全季/finished/fin)写 `S01` 不带 E。infer_audio_codec 取首个音频编码(DDP5.1/DTS5.1/TrueHD/Atmos/AC3/AAC/FLAC/LPCM)。build_card 加 audio 参数，extra 行=episode+quality+source+encode(视频H.265)+audio。实测用户链接：28 个视频 E01-E28 连续 → 写 `S01E01-E28`、audio=`DDP5.1`、带 Complete 时降级 `S01`，均符合预期。AUDIO_RE 初版 `DDP[\d.]+` 误吞尾点 → 改为 `DDP\d+(?:\.\d+)?`。容器已重启生效。等用户复测。

四轮：用户希望「分辨率/集数/编码」那行 + 「简介」整段能像截图那样带 Telegram 的「复制」按钮。修法是 build_card 内用 <pre></pre> 标签包两段——Telegram 对 <pre> 会渲染等宽字体并显示复制图标。本地改 build_card.py 412-413（extra 行）和 423-424（overview 段）→ scp 同步 → docker restart。容器内离线渲染确认两段都包好。等用户重新发链接验证。

模板本地化：用户要求标题给中文、评分格式 `X/10`、`115网盘` 蓝色可点击。修法：TMDB 改为按 multi search 拿原始语言为 zh 的条目，用 original_name（中文原名）作 display_title；评分加 `/10`；链接换成 HTML `<a href="...">115网盘</a>` 并把 parse_mode 改为 "HTML"。本地改 tmdb_search / build_card / 三个调用点 → 同步 → 渲染：标题「九门 (2026)」、评分 6.8/10、链接为蓝色锚。等用户复测。

海报发送 bug 修复：用户私聊发真链接后机器人回「⚠️ 卡片发送失败」。日志报错 `Input should be an instance of InputFile ... input_value=b'\xff\xd8...'`（裸 bytes）。根因：aiogram 3 的 send_photo 不接受裸 bytes，必须包成 InputFile。修复：新增 `from aiogram.types import BufferedInputFile`，在 _render_and_send_card 内 `photo_arg = BufferedInputFile(poster, filename="poster.jpg") if poster else None`，两处 send_photo 改传 photo_arg。本地改后 scp 到 NAS 并 docker restart p115-card-bot，新容器无导入错误、115登录成功、正常轮询。用户重新发链接即可看到带海报的卡片。

模板对齐用户参考截图（用户提两点反馈：1.标题要中文 2.「115网盘」要蓝色可点击；外加评分 6.8→6.8/10）。三处改动：(a) tmdb_search 改为先 search 再调详情 /tv/{id} 或 /movie/{id}，中文剧/电影（original_language==zh）优先用 original_name/original_title 作 display_name，否则用本地化名；评分格式改为 f"{rating:.1f}/10"。(b) build_card 改为生成 Telegram HTML：from html import escape as html_escape，新增 H(text)/HURL(href) 内嵌辅助，把所有用户/网络来源字段 escape，可变链接改为 `<a href="HURL(share_link)">115网盘</a>`。(c) _render_and_send_card 两处 send_photo/send_message 的 parse_mode=None 全部改为 parse_mode="HTML"。调用点 process_one(2处) + handle_card_text(1处) 引入 display_title = (info or {}).get("title") or parsed["title"] 传给卡片。同步+重启后端到端渲染：「Mystic.Nine.S01.2026.2160p.WEB-DL...」→ 标题「九门 (2026)」、评分 6.8/10、链接为 HTML 锚，标签 #九门 #剧情 #动作冒险，与用户截图模板字段一致。

六轮（用户要求：图片缓存一律不留，发送完成后直接立马删除）：海报本来就是 fetch_poster_bytes 下载的内存 bytes + BufferedInputFile 发送，从不写磁盘。加固：(1) 顶部 import gc；(2) _render_and_send_card 发送成功后 finally 块 `del photo_arg; del poster; gc.collect()` 强制回收内存；(3) 三处调用点(process_one 测试分支/正常分支 + handle_card_text) 发送后立即 `del poster` 释放调用方引用。容器内实证：下载海报到内存后 /cardbot、/tmp、/app 下 *.jpg/*.png 数量 before/after_dl/after_del 全 0（NO_DISK_CACHE=True），确认磁盘零残留。

七轮（转存+重命名+分享，核心 bug 已修复）：用户要求转存后重命名再分享，格式 {中文标题}.S{季}E{集}.{年}.{画质}.{源}.{音频}.{HDR}.{视频编码}.{ext}，范围=顶层文件夹+每个视频。新增 _rename_saved(svc,to_cid,title_cn,year) 与 save_rename_share(url,metadata,title_cn,year)。踩到两个 p115client 硬坑：(1) **fs_rename 每次只能改 1 个文件**（payload 是单个 (file_id,file_name) 元组/字典，不是 list），之前一次性传 list 只生效前 12 项（1 文件夹+11 视频）→ 必须 for 循环逐条 await，每条约 0.12s 限速；(2) parse_filename 的 ENC_RE 把音频编码(DDP5.1/DTS)和视频编码(H.265)混在一起，视频文件名里 DDP5.1 排在 H.265 前 → encode 被误取成 DDP5.1，名字出现重复 DDP5.1 且丢 H.265。修复：新增 extract_video_codec(name)（VIDEO_CODEC_RE 只匹配 H.26x/HEVC/AVC/x26x/AV1 等视频编码）；infer_audio_codec 改为取众数（most common），避免取到第一个文件的例外音频。SSH 用 cj962464@192.168.31.70（key ~/.ssh/id_ed25519_nas，此前用 root 被拒）。实测 to_cid=3498979337405728220：29/29 全部改名成功，文件夹=九门.S01E01-E28.2026.2160P.WEB-DL.DTS5.1.DV.H.265，28 个视频=九门.S01E{NN}.2026.2160P.WEB-DL.DTS5.1.DV.H.265.mp4。注意源发布混音（E28=DDP5.1 其余=DTS5.1），众数取 DTS5.1 统一标注。所有一次性脚本已清理，容器内仅剩 cardbot.py+docker-compose.yml。

八轮（命名规则按用户 115 现有风格统一）：用户给截图显示其 115 已有 24+ 个剧集文件夹全部用「名字 (年份) (tmdb-XXX)」格式（如 少年法庭 (2022) (tmdb-112833)、九门 (2026) (tmdb-271016)），并要求转存后单集加发布组后缀 -Cxuan。修改：(a) tmdb_search 返回 dict 加 tmdb_id 字段（从 search result 的 item_id 透传）；(b) build_canonical_name 加 suffix 参数，在 ext 前拼 -suffix；(c) 新增 build_folder_name(title_cn, year, tmdb_id) → `名字 (年) (tmdb-id)`，字段缺失时容错；(d) _rename_saved 加 suffix="Cxuan"（默认）与 tmdb_id 两个参数，文件夹走 build_folder_name（不再用 S/E/画质等技术元数据），单集走 build_canonical_name+suffix；(e) save_rename_share 加 tmdb_id；(f) process_one 透传 info["tmdb_id"]。新规则示例：文件夹 `九门 (2026) (tmdb-271016)`，单集 `九门.S01E01.2026.2160P.WEB-DL.DTS5.1.DV.H.265-Cxuan.mp4`。代码已同步容器并重启。

九轮（对已转存目录重命名+生成新分享）：对 to_cid=3498979337405728220 执行 _rename_saved(suffix="Cxuan", tmdb_id=271016)，29/29 成功；文件夹=九门 (2026) (tmdb-271016)，28 视频=九门.S01E{NN}...H.265-Cxuan.mp4。复用 svc.create_share_link(save_result)，save_result 需 {to_cid, names=[文件夹名], original_total_size, original_file_count:0, original_folder_count:0, have_vio:False}——file/folder 计数传 0 会让其退化为"只按体积判定"，避免源计数歧义导致永远等不到匹配。新分享链接：https://115.com/s/swsd6a53nkv?password=v2b9（长期有效）。注意：create_task_subdir 会在 115-Share 下留一层带时间戳的 task 子目录（如 Mystic.Nine..._20260819_160720），分享的是该子目录内的"九门"文件夹，故分享链接打开干净无时间戳目录。

十轮（清理废弃的 P115-Share 容器）：用户确认 p115-share 容器没用了。确认依赖关系：p115-card-bot 用的是同一镜像 listeningltg/p115-share:latest，但**独立容器+独立数据目录**，只复用镜像自带的代码包（P115Service/p115client），不连 p115share.db，也不走 8000 端口——所以 p115-share 容器停了不影响 card-bot。删除：docker stop+rm p115-share，rm -rf /vol2/1000/docker/115/data（P115-Share SQLite 库 + logs 共 1.5MB），rm /vol2/1000/docker/115/docker-compose.yml（该 compose 是给 p115-share 用的，card-bot 的 compose 在 /vol2/1000/docker/115/card-bot/docker-compose.yml 不动）。删除后 /vol2/1000/docker/115/ 下只剩 card-bot 目录；p115-card-bot Up 14 minutes 不受影响。注意：用户原本想加"等 115 分享审核好再发链接"的监测，被用户暂停（"先不考虑这个了"），原因是发起者/接收者视角 API 均不返回审核状态字段（share_info_app、share_snap_app、webapi share/snap 均无 audit 字段），无法精确判定。

十一轮（修标题误识别）：用户发链接 https://115cdn.com/s/swsdnu53wwq?password=m2f2 并截图：机器人把"耗时 1.6s 🚗 永V免费解锁：0/0 剩余0"当成了标题，画质/源/编码全空，TMDB 查不到。根因：HDHive 这种转发机器人会自动建 115 分享并把 share_title 设成它自己的"解锁状态文本"，但分享里**真实视频文件名是 Monkey.Wrench.S01E01.2022.1080p.NF.WEB-DL.AAC2.0.H.264-HiveWeb.mkv**（真名 Monkey Wrench 2022 1080p NF）。第一次修：base_name 优先级改为 user_name > 视频文件名(video_names[0]) > top_name > url。第二次发现更深 bug：HDHive 占位期间 share_snap_app 返回的 root_list 中 fn 也是占位字符串（如"耗时 1 6s ...mp4"，以 VIDEO_EXT 结尾被收进 video_names），导致 video_names 非空但全是占位，video_names[0] 仍解析出乱码标题。第二次修：加 SHARE_PLACEHOLDER_KW = re.compile(r"耗时|解锁|剩余\d|剩余\s*$|免费资源|HDHive 解锁")，process_one 开头对 fetch_share_info 轮询 4 次（间隔 8s，最多等 24s），如果 share_title 仍含占位关键词则不处理，直接回复用户"⏳ 这个分享还在 HDHive 处理中，请 1-2 分钟后再发链接"。

十二轮（修 Postedia "|" 误判 user_name + 标题中文名）：用户发新链接 swsdnpn3wwq（九门），截图显示机器人把"耗时 0 8s 🚗 永V免费解锁：0/0 剩余0"当标题。两个新根因：(a) Postedia_bot 转发的 caption 含 "💰 免费资源 | 耗时 0.8s 🚗 ..." 行，handle_message 按 "|" 拆分后把第二段当 user_name（自定义标题），优先级最高污染 base_name；(b) 标题中文 vs 英文的取舍。修复：(a) handle_message 解析 user_name 后立刻用 SHARE_PLACEHOLDER_KW 过滤，命中占位关键词则 user_name=""。标题最终定论：**display_title = TMDB 中文名(九门) 优先，查不到才用 parsed["title"] 原名(Mystic Nine) 兜底**；画质/源/编码/集数始终从真实视频文件名 parse（base_name 已优先 video_names[0]，避免占位乱码）。即「中文标题 + 真实元数据」组合。代码已同步容器并重启。

十三轮（切正式模式）：用户反馈"少了转存和重命名"——之前 CARD_BOT_TEST_MODE=2（只查信息不转存不发频道），卡片链接还是原始 url。已把 /vol2/1000/docker/115/card-bot/.env 的 CARD_BOT_TEST_MODE 改成 0，docker compose up -d 重建容器（17:32:32 启动、115 登录成功、printenv 确认=0）。现在走完整流程：转存→重命名(文件夹=中文名(年)(tmdb-id)，单集=完整元数据-Cxuan)→分享→发频道。

十四轮（修视频编码误识别 AAC）：用户发 Heart Signal 链接 swsdnox3np7，卡片复制行出现 "S09E01-E10 2160P WEB-DL AAC AAC"（AAC 重复）。根因：parse_filename 的 ENC_RE 混了音频编码(AAC/AC3/DTS/DDP 等)，视频文件名里 AAC 排在 H.265 前，enc 被误取成 AAC，与 infer_audio_codec 的 audio=AAC 重复。修复：ENC_RE 改为只匹配视频编码(x264/x265/HEVC/H.264/H.265/AVC/AV1)，音频编码完全交给 infer_audio_codec。验证：Heart.Signal 文件名 enc='H.265'、audio='AAC'，extra 行正确。注意：Heart Signal 是韩综，TMDB 里有多版本（韩版/日版/中国版），search "Heart Signal" 会误中"心脏信号 日版(2022)"，需要后续优化 TMDB 精确匹配（用年份/原名/季次过滤）。

十五轮（TMDB 多版本消歧 + 无文件夹分享自动建目录）：
(A) TMDB 消歧：probe 确认 search "Heart Signal" 返回 3 版本——日版(2022,pop1.1)、中国版《心动的信号》(2018,pop35.5)、韩版《心脏信号》(2017,pop4.8)，原 results[0] 误中日版。修复：parse_filename 新增 year 字段（正则 `(?<![A-Za-z0-9])(19\d{2}|20\d{2})(?![\dpPiI])`，排除 1080P/2160P 分辨率与 S06E42 季集号干扰；注意不能用 lookbehind 排除 `.` 因为 Do.2019 这种文件名年份前就是点）；新增 _score_tmdb_item（年份一致+100 决定性、名字/原名含翻拍标记(日版|中国版|Remake|US|Japan…) -30、人气微调）与 _pick_tmdb_item（若文件名带季数 S09，对前 3 高分候选查 /tv/{id} number_of_seasons，优先选 季数>=文件季数 的条目——韩版 9 季 vs 日版 2 季/中国版 5 季）；tmdb_search 签名扩为 (title, year="", season=None)，两处调用点(process_one + handle_card_text)透传 parsed["year"] 与 extract_episode 的季数。验证：Yes.I.Do.2019.S06（《喜欢你我也是》韩综，43 集）正确命中 tmdb-124265。
(B) 无文件夹分享（用户报 Labyrinth 单文件电影链接 swsdnn83wwq）：转存后 save_res["to_cid"]=task 子目录(share_title+时间戳)，其内直接是裸视频 → _rename_saved 原 `if not is_dir: continue` 全跳过 → 裸发。修复：_rename_saved 检测 top 无 dirs 有 files 时，fs_mkdir(build_folder_name(...)) 建规范文件夹 + fs_move 移入，返回第 4 个值 new_cid；save_rename_share 拿 new_cid 后更新 save_res["to_cid"]/["names"]，让 create_share_link 分享新文件夹。build_canonical_name 支持 season=None（电影无 S/E 段，如 `迷宫.2025.1080P.WEB-DL.AAC2.0.H.264-Cxuan.mkv`）；视频无集数不再 continue 跳过而是走电影命名，且用 used 集合防重名。容器已重启，代码已同步。
另：读取了全局工作台 C:\Users\15102\Desktop\Workspace\00_全局工作台（最高优先级指令/全局工具台/新项目SOP/踩坑日志），规则为项目必须放 Project-NNN 目录——本项目为既有项目（D:\小龙虾\115分享转发），符合"已有项目进原目录"规则，不新建目录。

十六轮（修"4 个链接只推送 1 个"——链接被吞）：用户发 4 个 HDHive 链接，只有 swsk9tg3wwq 进入转存并发布，其余被吞。排查日志找到**两个叠加根因**：(1) **Telegram 转发消息带 RTL 控制字符**（\u2066/\u2069 包裹 URL、\u200b 零宽空格、\ufeff BOM），LINK_RE 匹配时 URL 被截断/吞入乱码——实测 case: `\u2066https://115.com/s/xxx?password=m2f2\u2069` 匹配出带 `\u2069` 的 URL；链接中间夹 `\u200b` 被截断成 `https://115.com/s/swsk`（判"无效链接"）；链接后跟 emoji 被 `[^\s]+` 吞入。115.com 域名链接带这些字符时 LINK_RE 直接匹配失败 → 回"请发送有效的 115 分享链接"；(2) **message.answer 无异常保护**：18:20:45 处理 update 970163949 时 Telegram API 网络抖动（aiohttp TelegramNetworkError）在 handle_message 的 answer("⌛️ 正在处理...") 处抛异常 → 整个 handler 直接退出 → 该消息里所有 real 链接的 process_one 从未执行 → **链接被静默吞掉**（不是进程死，容器一直 Up）。修复：(a) 新增 _clean_share_text(full) 在 LINK_RE 提取前清除 \u200b\u200c\u200d\u2066-\u2069\ufeff 控制字符；(b) 新增 _URL_TAIL_RE = [^A-Za-z0-9?=&:/._-]+$ 剥离 URL 尾部被吞入的 emoji/符号；(c) handle_message 所有 message.answer / message.reply 包 try/except，失败仅 log 告警绝不中断转存；status 允许为 None（process_one 内 edit_text 已有容错）。本地验证 4 种 HDHive 真实消息格式全部提取出干净 URL。代码已同步容器并重启（18:28:48）。

十七轮（修"没转存就直接分享"——严禁行为）：用户发链接 swskam43np7（Getting.On 合集），日志实锤：20:18 用户密集发 6 条链接（20:18:04-12），115 风控"操作太频繁，请稍候再试"打断该链接转存（task 子目录已建好，share_receive 被拒），`save_rename_share` 失败。**根因**：process_one 原代码 `save_rename_share` 失败/异常时 `share_link = url`（fallback 用原始链接）→ 照常发卡片 → 未转存的原始链接流入频道。**这是用户严禁的行为**。三重修复：(a) 新增 `_is_rate_limited(res)` 判断 115 频控错误（频繁/稍候再试/频率/被限制/限流）；(b) 新增 `_save_share_with_retry(svc,url,metadata)` 包装 `_save_share_link_internal`，遇频控按 `_SAVE_RETRY_DELAYS=(10,20,30)` 退避重试 3 次，非频控错误直接抛给上层；(c) `save_rename_share` 转存步骤改用 `_save_share_with_retry`；(d) process_one 正常模式转存段包进**全局串行锁 `_SAVE_LOCK`**（asyncio.Lock）——批量链接排队逐个转存，从根源避免并发触发 115 频控；(e) **失败分支不再 fallback 原始链接**：`share_link` 为 None 时改发"❌ 转存+重命名+分享失败：{原因}\n请稍后重发（115 频控时建议间隔 30 秒以上）"并 return，绝不发卡片。代码已同步容器并重启（20:30 左右）。

十八轮（失败自动重试——全自动流程）：用户要求失败后**不要让他重发**，要全自动。新增：全局 `_RETRY_QUEUE=[]`（内存重试队列）+ `_RETRY_MAX=5`（单项目最多重试轮数）+ `_RETRY_INTERVAL=60`（worker 扫描间隔）+ 全局 `_bot`。新增 `_retry_worker()` 后台任务（main 启动时 `asyncio.create_task`）：每 60s 扫描队列，队首项目在 `_SAVE_LOCK` 内重跑 `save_rename_share`（转存→重命名→分享）；成功→重新下载海报 `_render_and_send_card` 推频道 + 私聊通知"✅ 之前失败的链接已自动重试成功"+ 出队；失败→`retry_count+1`，超 `_RETRY_MAX` 出队并私聊通知"⛔ 自动重试 5 轮仍失败已放弃"，否则移到队尾继续。process_one 失败分支改为把完整卡片参数（url/user_name/display_title/year/tmdb_id/poster_url/genres/rating/overview/quality/source/episode/encode/audio/size_text/user_id/retry_count）塞进 `_RETRY_QUEUE`，status 提示"⏳ 已自动加入重试队列，成功后自动推送卡片，无需重发"；`user_id` 提前到 info 段定义。绝不用原始链接发卡片。代码已同步容器并重启（20:32:42，worker 启动日志正常）。

十九轮（修遮天识别错——扩展名残留+混合包+100集以上集数）：用户发链接 swswt4y3w1n，卡片标题"遮天 背棺战王腾 2026 mkv"带扩展名、TMDB 全空。probe 实锤三个根因：(a) **parse_filename 不剥离视频扩展名**：`遮天 背棺战王腾.2026.mkv` 尾部年份正则被 `.mkv` 挡住，标题残留 "2026 mkv" → 修复：parse_filename 开头先 `video_ext` 剥离扩展名再做全部解析（quality/source/enc/EP_RE/year 都基于去扩展名的 base）；(b) **混合分享包**：169 视频 = 167 集主体动画（遮天.2023.S01E001-E167...2160P.WEB-DL.H.265）+ 2 个剧场版散件（遮天 背棺战王腾.2026.mkv/mp4），`video_names[0]` 撞上散件 → 修复：process_one 与 _rename_saved 都加**结构众数选主体**（collections.Counter 统计含 SxxEyy/第N集/年份 的文件名结构指纹，取众数标记所在文件为主，>3 文件才启用）；(c) **分享根名自带 {tmdbid-XXXX} 精确标记**（top_name="Z 遮天{tmdbid-224839}."）没利用 → 新增 parse_tmdbid_marker() 提取，tmdb_search 加 tmdb_id 参数（有标记直接按 id 查 tv/movie 详情，绕过搜索，比消歧可靠）；tmdb_search 详情段抽成 _tmdb_detail(media_type,item_id,item) 复用；(d) 顺手发现 **EP_RE 集数上限 2 位**：`S01E167` 被贪婪拆成 `E16-E07` → 全部 `\d{1,2}` 改 `\d{1,3}`（S/E 范围、第N集、E 裸号统一）。实测（容器内真实 probe）：base_name=遮天.2023.S01E167...、title=遮天、quality=2160P、source=WEB-DL、encode=H.265、year=2023、tmdbid marker=224839、TMDB 命中（遮天 224839 动画 8.9/10 海报齐）。注意 EP_RE 是字符串正则，容器 grep 检查要转义（`EP_RE = ` 不带括号）。代码已同步容器并重启（22:50 前）。

二十轮（修重器海报缺失+命名不完整）：用户发 swsdhps3nkv，日志显示 TMDB 海报 URL 正确但 22:43:24 `image.tmdb.org` 网络连接失败，原海报下载无重试；新增 `_aio_get_bytes` 1/3/7 秒退避、最多 4 次下载重试，实测该海报下载成功 121412 bytes。命名问题：转存后源 22 个视频，首次列目录只拿到 17 项且 `_get_dir_items` 报分页重复，导致只重命名 18 项（文件夹+17），E10-E22 原名未改，E01-E05 因重复页被序号保护污染成 `-10/-11/-15/-16/-17`。新增 `_get_ready_dir_items`：转存后等待目录落齐（至少 8 秒，最多 40 秒），按文件 ID 去重；支持 expected_video_count，必须收集到源视频数才结束等待。`_rename_saved` 支持 expected_video_count，process_one/重试 worker 透传 `len(video_names)`。对现有 to_cid=3499178681627051877 补偿执行：23/23 成功（文件夹+22视频），最终文件夹 `重器 (2026) (tmdb-291856)`，视频 E01-E22 全部为 `重器.S01E{NN}.2026.2160P.WEB-DL.DDP5.1.H.265-Cxuan.mkv`，无污染后缀。代码已同步容器并重启，临时脚本已清理。

二十一轮（同步项目到桌面工作台）：用户要求“同步本项目到桌面”。按全局工具台与新项目 SOP，检查桌面已有 Project-001 至 Project-005 后创建连续编号 `C:\Users\15102\Desktop\Workspace\Project-006-115分享转发`，建立标准八目录与项目管理文档。同步内容：`03_代码与配置/card-bot/`（cardbot.py、docker-compose.yml、.env.example）、`03_代码与配置/p115-src/`（源码、API 配置、压缩包、probe.py）和根目录 Docker compose 副本。明确排除 `.env`、凭据、`__pycache__`、`.workbuddy` 运行记忆；源项目未删除、桌面其他项目未改动；核对确认同步目录无敏感/运行时文件。

二十二轮（ed2k 只读测试）：用户要求像 `ed2k://|file|Svodish.s.uma.2025.WEB-DL.1080p.ExKinoRay.mkv|4623060042|...|/` 这样的链接直接识别，但明确要求先测试、不得发送。当前代码 `LINK_RE` 只支持 115/115cdn/anxia，`LINK_RE` 对 ed2k 返回 False，尚未改正式流程。只读解析结果：文件名 `Svodish.s.uma.2025.WEB-DL.1080p.ExKinoRay.mkv`，标题 `Svodish s uma`，年份 2025，画质 1080P，源 WEB-DL，文件大小 4623060042 bytes，hash 已读取；TMDB 命中《爱上平行时空的你》(tmdb-1489187)，2025，喜剧/爱情/奇幻，7.5/10，海报和简介可用。没有转存、重命名、创建分享或发频道。等待用户确认后再把 ed2k 接入测试/正式流程。

二十三轮（ed2k 私聊卡片测试）：用户要求把该 ed2k 通过私聊发卡片查看，仍不接入正式监听、不转存、不发频道。一次性测试脚本使用已验证的 TMDB 元数据渲染卡片，并将 ed2k 原链放进可复制 `<pre>`；首次 Telegram 发送因 NAS 代理连接重置失败，加入 0/3/8 秒发送重试后第三次成功，私聊卡片已发送。由于同一时段到 `api.themoviedb.org`/`image.tmdb.org` 的代理持续连接失败，本次卡片为文字版（海报 4 次重试仍未下载），正式代码未修改。随后用户要求重新发送带海报版本：从本机直连 image.tmdb.org 下载已确认海报（500x750 JPEG，109873 bytes），上传容器后通过 Bot 私聊发送，首次即成功（SENT_OK 1）；未发频道、未改正式监听。发送后本机/NAS/容器的临时海报与脚本全部清理，容器验证 CLEAN_OK。

二十四轮（ed2k 并入正式完整流程）：用户确认测试卡片可用，要求正式并入。新增 `ED2K_RE`（支持 URL 编码/文件名空格，格式 ed2k://|file|name|size|32位hash|/）、`_extract_links`（统一提取 115+ed2k 并保序去重）、`_parse_ed2k`（解析 filename/size/hash）。handle_message 改为支持 ed2k，且 ed2k 自带 `|` 分隔时跳过 115 的“链接|自定义名”解析，避免文件名被当 user_name。process_one 最前新增 ed2k 专用分支：解析文件名→TMDB→海报→构造卡片，原链放 `<pre>` 可复制；**ed2k 不属于 115 转存体系，严禁进入 save_rename_share，直接发送频道+用户私聊后 return**。频道和私聊分别 0/3/8/15 秒重试，防一端失败导致重复发送；海报继续使用 4 次下载重试。/help 加 ed2k://。语法检查及回归测试通过：普通 ed2k、尾#、文件名空格、115+ed2k 混合消息均正确，115 链接识别未回归。代码已部署 NAS，容器 23:44:53 重启健康。未用样例再次发频道，避免重复发布。

二十五轮（115发送成功后20分钟自动清理）：用户要求“原来的先不删除；以后发送成功20分钟后自动删除”。只读盘点发现115-Share历史29个任务目录，用户明确不删除，未加入任何队列。确认 `p115client.fs_delete` 仅移入115回收站，`recyclebin_clean*` 才是永久清空；代码只调用 fs_delete，不调用永久清空接口，回收站密码不写入代码/日志。新增持久化 `_cleanup_queue`（状态文件 `/cardbot-state/cleanup_queue.json`）与 `_schedule_cleanup`、`_cleanup_worker`：只有频道卡片发送成功后才记录外层任务目录 CID，20分钟后自动调用 fs_delete；频道发送失败不计时、不删；worker 每30秒扫描，失败后60秒重试；容器重启会加载队列。`save_rename_share` 返回 cleanup_cid/cleanup_name，正常流程和自动重试成功流程均接入；新增 compose 状态卷 `/vol2/1000/docker/115/card-bot/state:/cardbot-state`。重建容器后日志确认：自动清理队列加载0项（历史目录不在队列）、115登录成功、状态卷挂载生效。原有29个目录保持不动。