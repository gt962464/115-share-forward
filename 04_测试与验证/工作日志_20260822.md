- 用户要求同一消息内同一部剧同一季的多个ED2K链接合并到一张卡片（示例 Silo S03E01-E08）。新增 `_ed2k_group_key` 按规范化剧名+年份+季数分组；同组只生成一张海报卡片，集数合并显示、大小合计；完整ED2K链接列表作为后续文本消息发送，避免Telegram图片说明长度限制；不同剧/季保持独立。8条Silo测试全部分到同一组。已部署NAS并重启（01:01:23，数据库表结构确认、115登录成功），代码同步桌面，代码与桌面一致，SHA256 `bf415c6ddcc29eae78ee3ba5f70a392f9230d0e6902eb330adf00b0aa3a6c03b`。
- 修复ED2K合并报错：完整链接列表标题原先直接对季号正则结果调用 `.group(1)`，未匹配时触发 `NoneType.group`；现改为安全 `season_match`，匹配不到显示“本季”。语法通过，NAS 01:05:11 重启健康、115登录成功，桌面同步，SHA256 `7e8cbf519c428d6bdaf7372920b99a0f0e2f023c8dd95fd7b139928fdeb5dc31`。